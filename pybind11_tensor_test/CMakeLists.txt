# CMakeList.txt : CMake project for pybind11_tensor, include source and define
# project specific logic here.
#
cmake_minimum_required (VERSION 3.8)

# Add source to this project's executable.
add_executable (pybind11_tensor_test "pybind11_tensor_test.cpp" "pybind11_tensor_test.h")

# TODO: Add tests and install targets if needed.
